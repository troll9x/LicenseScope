# Artifact verification

Verify the six-file allowlist, checksums, release manifest, Setup version/signature policy and SPDX SBOM. GitHub attestations, if later enabled after signing and final hashing, can be checked with `gh attestation verify`; repository visibility/plan and CLI authentication are `NEEDS REMOTE VERIFICATION`.
